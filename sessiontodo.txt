# 会话全量清单 — 自接手以来的改动与待办

> **用途**：本会话（分支 `arena/019fd62f-mixin-helper`）的所有工作，逐条列成 todolist。
> 因为改动**尚未推送到 GitHub**（origin/main 仍停在基线 `74f10e9`），新对话基于远程仓库时看不到这些内容，故完整记录于此。
> 状态：工作树干净，4 个本地提交积压（见第 3 节）。

---

## A. ✅ 已改的地方（共 26 个文件，+1501/−353）

### A1. 新增文件（4 个核心解析器 + 1 交接文档）

- [x] **`domain/service/AsmDescriptorParser.kt`**（新增）
  - JVM 描述符解析：`(Lnet/minecraft/...;I)V` → 参数类型列表 + 返回类型；支持数组/对象/基本类型
  - 供 TinyParser、ParchmentParser 使用
- [x] **`domain/service/MojmapParser.kt`**（新增）
  - 完整解析 Mojang client_mappings.txt（ProGuard 格式）的**类 / 方法 / 字段**
  - 原实现只解析类且 `take(500)` 截断 → 现全量解析，填充 descriptor / params / returnType
- [x] **`domain/service/TinyParser.kt`**（新增）
  - 解析 Yarn mappings.tiny 的 **Tiny v1 / v2** 两种格式
  - 描述符中的类名（official → named）自动重映射为可读名
- [x] **`domain/service/ParchmentParser.kt`**（新增）
  - 解析 parchment.json（Feather MDC 官方规范）：类/方法/字段 Javadoc + 参数名
  - 与 Mojmap 结果合并；参数 index 正确处理 this 占位、long/double 双位、static 回退
- [x] **`HANDOFF.md`**（新增）— 交接文档（操作指引 + 技术坑），本清单是它的补充细化版

### A2. 修改文件（核心逻辑）

- [x] **`data/repository/MappingRepository.kt`**（改动最大，+250 行左右）
  - 版本列表**多源获取**：Mojang 正式版(40) + Fabric 稳定版(30) + Forge(10) + NeoForge(10) + Parchment 条目
  - 单源失败不影响其他源；刷新保留"已缓存"标记；版本号数字元组排序（修复字符串字典序 bug）
  - `downloadAndParseMappings` 按 mappingType 编排：yarn / mojmap / parchment 三分支
  - `search()`：FTS4 前缀匹配优先 + LIKE 回退，支持版本/类型过滤
  - `decideMappingType` 改为数字比较（仅兜底用）
- [x] **`data/remote/MappingDownloader.kt`**
  - `downloadMojangMappings`：真实下载 + 26.x 无 client_mappings 时明确报错
  - `downloadYarnMappings`：meta.fabricmc.net 查最新稳定版 → 下载 yarn jar → 解压 `mappings/mappings.tiny`
  - `downloadParchmentJson`：maven.parchmentmc.org 查版本 → 下载 zip → 解压 `parchment.json`
- [x] **`data/local/AppDatabase.kt`**
  - **迁移 v1→v2**：mappings 加 version/loader 列 + 索引；versions 改复合主键 id(version|loader) + versionJsonUrl 列
  - **迁移 v2→v3**：mappings 加 paramNames / javadoc 列
  - 移除 `fallbackToDestructiveMigration`
- [x] **`data/local/MappingEntity.kt`** — 加 `version` / `loader` / `paramNames` / `javadoc` 列 + 索引
- [x] **`data/local/VersionEntity.kt`** — 复合主键 `id=version|loader` + `versionJsonUrl` 列
- [x] **`data/local/MappingDao.kt`**
  - 搜索 SQL 重写：`WHERE id IN (SELECT rowid FROM mappings_fts WHERE mappings_fts MATCH ...)`（修复别名混用）
  - 新增 `getDownloadedVersions()`（搜索页版本选择器数据源）
- [x] **`data/local/Converters.kt`** — 空串边界修复（`takeIf { isNotEmpty() }`，原返回 `[""]`）
- [x] **`data/remote/MojangApi.kt`** — VersionEntry 加 `type` 字段（区分 release/snapshot）
- [x] **`data/remote/FabricApi.kt`** — 加 `getYarnVersions` 与 `stable` 字段
- [x] **`di/AppModule.kt`** — 注册迁移链 `addMigrations(MIGRATION_1_2, MIGRATION_2_3)`

### A3. 修改文件（UI 层）

- [x] **`ui/dashboard/DashboardViewModel.kt`**
  - 状态机重构：`Success(message)` / `Error(message)` 持久保留；`refreshVersions()`；`downloadMappings(version, loader)` 用真实 entity（含真实 versionJsonUrl）
- [x] **`ui/dashboard/DashboardScreen.kt`**
  - Loader 过滤版本下拉（Mojang/Fabric/Forge/NeoForge/**Parchment**）；"✓ 已缓存"标记；刷新按钮
  - 状态持久卡片（成功/失败）+ 失败时"重试"按钮
- [x] **`ui/search/SearchViewModel.kt`**
  - 250ms 防抖实时搜索；query/type/version 三状态流；下载版本列表加载
- [x] **`ui/search/SearchScreen.kt`**
  - 版本范围选择（全部/已下载版本）；FTS 前缀自动补全；空态提示；搜索 loading 指示
  - 点击结果弹**详情对话框**：类型/版本/类/混淆名/描述符/参数/参数名/返回类型/Javadoc + 复制名称/描述符
- [x] **`MainActivity.kt`**
  - 底部导航修复：`currentBackStackEntryAsState()` 正确高亮 + `launchSingleTop`/`restoreState`（原写死永远高亮第一项）

### A4. 删除文件（产品决策）

- [x] **`ui/mixin/MixinConfiguratorScreen.kt`**（删除）
- [x] **`domain/service/MixinTemplateRenderer.kt`**（删除，生成的代码含无效 `@Local` 注解）
- [x] **`domain/model/AsmDescriptor.kt`**（删除，死代码）
  - 理由：Mixin 代码示例生成器对目标用户无用（会写的不需要模板，不会写的用不上）
  - **保留** `AsmDescriptorBuilder.kt`（Mojmap/Tiny 解析器仍在用）

### A5. 文档

- [x] **`README.md`** — 功能列表更新（Yarn/Parchment/搜索/迁移/状态反馈）
- [x] **`TODO.md`** — 原审查清单勾选状态更新（P0 全部完成，P1/P2 大部分）

---

## B. ⬜ 未改 / 准备改的地方（待办）

### B1. 必须（新 session 第一件事）

- [ ] **推送 + 开 PR**（当前 4 个提交积压本地，GitHub 完全看不到）
  ```bash
  git push origin arena/019fd62f-mixin-helper
  gh pr create --base main --head arena/019fd62f-mixin-helper \
    --title "完善映射下载与搜索：多源版本列表 / Mojmap / Yarn / Parchment" \
    --body "见 HANDOFF.md 第 2 节"
  ```

### B2. 工程质量

- [ ] **补单元测试**（当前 0 测试）
  - 优先覆盖纯逻辑：`AsmDescriptorBuilder`、`MojmapParser`、`TinyParser`、`ParchmentParser`、版本号比较
  - 参考：本会话用 Python 1:1 复刻解析逻辑并跑断言全部通过（脚本在沙箱 /tmp，未入库，可按需重写为 JUnit）
- [ ] **CI 打磨**
  - `gradle-wrapper.jar` 目前靠 CI 里 curl 下载，建议直接提交 jar 入库（1MB 内 GitHub 允许）
  - workflow 可加 `./gradlew test` 步骤跑单测

### B3. 可选优化

- [ ] 搜索详情加"**复制 Mixin 目标**"按钮：拼 `@Inject(method = "tick()V", at = @At("HEAD"))` 可直接粘贴字符串（一行代码，不做不影响）
- [ ] `data/local/VersionDao.kt` 无实质改动（仅格式），可忽略

### B4. 已知行为（非 bug，勿"修"）

- [ ] **26.x 起新版 MC 的 version.json 已不再附带 client_mappings**（Mojang 官方停止发布）→ 下载 Mojmap 会明确报错并提示改用 Fabric/Yarn
- [ ] **Parchment 并非所有版本都有数据** → maven.parchmentmc.org 查不到时明确报错
- [ ] FTS4 前缀匹配写法：`MATCH "\"词\"*"`，输入需清洗特殊字符（已实现）

---

## C. 提交状态速览

```
74f10e9  基线（origin/main 当前指向，即你看到的 README PR 所在）
e6b36b2  支持 Parchment 映射（参数名 + Javadoc），完成中优先级任务 7（含此前的全部工作）
93b7f18  更新 TODO 清单状态：标记已完成项
5c75c1d  按产品决策移除 Mixin 代码示例生成器
04434a8  新增 HANDOFF.md 交接文档
```
- 分支：`arena/019fd62f-mixin-helper`（勿换分支）
- 工作树：干净
- 新 session 指引：让 agent 读 **HANDOFF.md**（操作步骤）→ 本文件（明细清单）→ TODO.md（遗留待办）

---

*生成于 2026-08-06，本会话收尾。*